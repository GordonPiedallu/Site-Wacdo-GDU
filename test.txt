<div id="choix-overlay">
             <img src="images/supprimer.png" id="imgfermer" alt="Croix de fermeture">
            <h1 id="overlay-title">Une petite soif ?</h1>
            <p id="overlay-text">Choisissez la taille de votre boisson,  +0.50€ pour le format 50 Cl</p>
            <div id="icone-choix">
            <a class="choix-menu" id="choix-pboisson" href="#"><img src="boissons/coca-cola.png" alt="Boisson 30Cl">30 Cl</a>   
            <a class="choix-menu" id="choix-gboisson" href="#"><img src="boissons/coca-cola.png" alt="Boisson 50Clm">50 Cl</a>
            </div>
         <div id="compteur">
            <button type="button" id="btn-moins">-</button>
            <span id="quantite">1</span>
            <button type="button" id="btn-plus">+</button>
        </div>
        <div id="bouton-overlay">
            <button type="button" id="btn-annuler">Annuler</button>
            <button type="button" id="btnajout-panier">Ajouter a ma commande</button>
        </div>
        </div>>


         const overlayModel = document.createElement('div');
                overlayModel.id = 'choix-overlay';
                const imgFermer = document.createElement('img');
                imgFermer.src = 'images/supprimer.png';
                imgFermer.id = 'imgfermer';
                imgFermer.alt = 'Croix de fermeture';
                overlayModel.appendChild(imgFermer);
                const h1 = document.createElement('h1');
                const p = document.createElement('p');
                overlayModel.classList.add('h1');
                h1.textContent = 'Une petite soif ?';
                overlayModel.appendChild(h1);
                overlayModel.classList.add('p');
                p.textContent = 'Choisissez la taille de votre boisson,  +0.50€ pour le format 50 Cl';
                overlayModel.appendChild(p);
                overlayModel.classList.add('icone-choix');
                const choixPBoisson = document.createElement('a');
                choixPBoisson.classList.add('choix-menu');
                choixPBoisson.href = '#';
                const imgPBoisson = document.createElement('img');
                imgPBoisson.src = 'boissons/coca-cola.png';
                imgPBoisson.alt = 'Boisson 30Cl';
                choixPBoisson.appendChild(imgPBoisson);
                overlayModel.appendChild(choixPBoisson);
                const choixGBoisson = document.createElement('a');
                choixGBoisson.classList.add('choix-menu');
                choixGBoisson.href = '#';
                const imgGBoisson = document.createElement('img');
                imgGBoisson.src = 'boissons/coca-cola.png';
                imgGBoisson.alt = 'Boisson 50Cl';
                choixGBoisson.appendChild(imgGBoisson);
                overlayModel.appendChild(choixGBoisson);
                overlayModel.classList.add('compteur');
                const btnMoins = document.createElement('button');
                btnMoins.type = 'button';
                btnMoins.id = 'btn-moins';
                btnMoins.textContent = '-';
                overlayModel.appendChild(btnMoins);
                const quantiteSpan = document.createElement('span');
                quantiteSpan.id = 'quantite';
                quantiteSpan.textContent = '1';
                overlayModel.appendChild(quantiteSpan);
                const btnPlus = document.createElement('button');
                btnPlus.type = 'button';
                btnPlus.id = 'btn-plus';
                btnPlus.textContent = '+';
                overlayModel.appendChild(btnPlus);
                overlayModel.classList.add('bouton-overlay');
                const btnAnnuler = document.createElement('button');
                btnAnnuler.type = 'button';
                btnAnnuler.id = 'btn-annuler';
                btnAnnuler.textContent = 'Annuler';
                overlayModel.appendChild(btnAnnuler);
                const btnAjoutPanier = document.createElement('button');
                btnAjoutPanier.type = 'button';
                btnAjoutPanier.id = 'btnajout-panier';
                btnAjoutPanier.textContent = 'Ajouter à ma commande';
                overlayModel.appendChild(btnAjoutPanier);